import 'package:flutter/material.dart';

class TimelineView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('时间轴视图 - 开发中'),
    );
  }
}